# M0-014 Windows socket copy profile

Status: **IN PROGRESS**

This directory starts the native Windows evidence path. The first hosted run is
an environment and instrumentation-capability audit on `windows-2025`. It does
not claim that the copy profile passed. A complete result must satisfy the strict
validator described by the [test plan](test-plan.md).

Long-duration SSE and release soak gates are outside this task and are not run.
