`result.json` uses schema_version=2. Each case retains alternating paired order,
raw `std.net` and `StdNetTransport` warmups, eleven measured samples, aggregates,
threshold decisions and one separate native instrumented sample per path.
