{
  "schema_version": 1,
  "task_id": "M7-029",
  "source_register_sha256": "884443de8316bf00cd0cbadcbfe8c77d6a2eb8743dd48c0003b1af726e745e39",
  "audited_baseline_commit": "49f30940875bd76876b4b46f1b115e5a9f31fd76",
  "review_target_sha256": "155f067110625b5d316f6bb6255836c17c080da560098b5150646bfd39698f0b",
  "policy": "Every Critical and High finding remains release-blocking until current-tree evidence proves Fixed or NotApplicable.",
  "findings": [
    {"id":"WS-BUILD-001","severity":"Critical","status":"FixInProgress","current_tree":"provider manifest is repository-visible; clean-checkout execution remains required"},
    {"id":"WS-CI-001","severity":"Critical","status":"FixInProgress","current_tree":"PR #94 triggered the canonical GitHub-hosted job; the first run exposed an unavailable historical nightly pin. The workflow now validates the latest complete Linux x64 nightly and binds that exact version for each run; a successful rerun and required-check ruleset evidence remain required"},
    {"id":"WS-EVID-001","severity":"High","status":"FixInProgress","current_tree":"M7-021 qualification binds native manifests, C/H sources, build scripts, build.cj, lockfile and manifest; regenerated release evidence remains required"},
    {"id":"WS-GOV-001","severity":"High","status":"OpenExternal","current_tree":"GitHub main is unprotected and rulesets are empty; the selected GitHub-hosted runner removes the self-hosted runner prerequisite but does not configure required checks"},
    {"id":"WS-H2-SERVER-FLOW-001","severity":"High","status":"FixInProgress","current_tree":"server transfers the reserved DATA permit to the writer; Http2WriteSchedulerTest passed 10 focused cases; final digest binding and independent retarget remain required"},
    {"id":"WS-H2-SETTINGS-001","severity":"High","status":"FixInProgress","current_tree":"RFC peer defaults, local caps and advertised settings are separated and server ENABLE_PUSH is prohibited; Http2SettingsTest passed 9 focused cases; final digest binding and independent retarget remain required"},
    {"id":"WS-H2-WRAP-001","severity":"High","status":"FixInProgress","current_tree":"cancellation wrapper preserves the delegate completion policy; HttpFacadeTest passed 14 focused cases; final digest binding and independent retarget remain required"},
    {"id":"WS-LIC-001","severity":"High","status":"FixInProgress","current_tree":"root Apache-2.0 LICENSE, THIRD_PARTY_NOTICES and exact AWS-LC LICENSE/NOTICE are repository-visible release inputs; release manifest and SPDX unit tests bind their digests, while final artifact regeneration remains required"},
    {"id":"WS-PROXY-001","severity":"High","status":"FixInProgress","current_tree":"authenticated CONNECT tunnels fail closed to one lease when no stable credential identity exists; Http1ProxyClientPipelineTest passed 2 focused cases; final digest binding and independent retarget remain required"},
    {"id":"WS-RES-001","severity":"High","status":"FixInProgress","current_tree":"close stops admission and returns without joining libc/NSS calls; a detached native reaper quarantines each pool, while process-wide admission caps live pools at 8 and workers at 64. Fault injection proves fail-closed overload and recovery after reaping; final independent retarget remains required"},
    {"id":"WS-RETRY-001","severity":"High","status":"FixInProgress","current_tree":"write timeout is Never retryable when zero physical commit cannot be proven; StdNetTransportTest passed 19 focused cases; final digest binding and independent retarget remain required"},
    {"id":"WS-STDNET-001","severity":"High","status":"FixInProgress","current_tree":"accept observes cancellation through a bounded wait quantum without closing the shared listener; StdNetTransportTest passed 19 focused cases including listener reuse; final digest binding and independent retarget remain required"},
    {"id":"WS-TLS-TRUST-001","severity":"High","status":"FixInProgress","current_tree":"bundle and hashed-directory source identities bind bounded deterministic SHA-256 content; LinuxSystemTrustAdapterTest passed 5 focused cases; final digest binding and independent retarget remain required"}
  ]
}
