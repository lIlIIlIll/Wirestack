# `wirestack.tls.TlsClientContextStore`

[包 `wirestack.tls`](../packages/package-32303a636a646f633a6d6f64756c653a76313a726f6f7477697265737461636b2e746c73.md)

```cangjie
public class TlsClientContextStore
```

Atomically replaceable client context; existing connections retain their snapshot\.

## 成员

### `wirestack.tls.TlsClientContextStore.init`

```cangjie
public init(initial: TlsClientContext)
```

Creates a store with the context used for the first handshake\.

#### 参数

- `initial` — Initial immutable client context\.

- constructor · public
- `src/tls/facade.cj:203:5`

### `wirestack.tls.TlsClientContextStore.handshake`

```cangjie
public func handshake(
        transport: DuplexTransport,
        serverName: HostName,
        context!: OperationContext = OperationContext.background()
    ): TlsConnection
```

Handshakes with one atomic context snapshot\.

#### 参数

- `transport` — Connected transport to secure\.
- `serverName` — Peer name used for certificate verification\.
- `context` — Handshake deadline, cancellation and tracing scope\.

- function · public
- `src/tls/facade.cj:222:5`

### `wirestack.tls.TlsClientContextStore.replace`

```cangjie
public func replace(value: TlsClientContext): UInt64
```

Replaces only future\-handshake context and returns its new epoch\.

#### 参数

- `value` — Immutable client context for subsequent handshakes\.

- function · public
- `src/tls/facade.cj:212:5`

### `wirestack.tls.TlsClientContextStore.snapshot`

```cangjie
public func snapshot(): TlsClientContext
```

Returns the immutable context used by the next handshake\.

- function · public
- `src/tls/facade.cj:206:5`

