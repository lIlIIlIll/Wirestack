# `wirestack.http.HttpClient`

[包 `wirestack.http`](../packages/package-32303a636a646f633a6d6f64756c653a76313a726f6f7477697265737461636b2e68747470.md)

```cj
public class HttpClient <: Resource
```

HTTP facade\. HTTPS initializes the build\-selected TLS handshaker lazily\.

## 成员

### `wirestack.http.HttpClient.builder`

```cj
public static func builder(): HttpClientBuilder
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- function · public
- `src/http/client.cj:270:5`

### `wirestack.http.HttpClient.close`

```cj
public func close(): Unit
```

Wirestack 公开 API：close。

- function · public
- `src/http/client.cj:580:5`

### `wirestack.http.HttpClient.connectWebSocket`

```cj
public func connectWebSocket(
        url: HttpUrl,
        protocols!: Array<String> = [],
        headers!: HttpHeaders = HttpHeaders(),
        webSocketLimits!: WebSocketLimits = WebSocketLimits(),
        context!: OperationContext = OperationContext.background()
    ): WebSocketConnectResult
```

Establishes an uncompressed WebSocket over HTTP/1\.1 or negotiated HTTP/2\.
Rejected handshakes return an owned response\. No redirects or retries replay the handshake\.

#### 参数

- `url` — Absolute HTTP or HTTPS URL of the WebSocket resource\.
- `protocols` — Ordered acceptable subprotocols\.
- `headers` — Additional headers excluding protocol\-managed fields\.
- `webSocketLimits` — Session frame, message and close bounds\.
- `context` — Shared handshake deadline and cancellation\.

- function · public
- `src/http/client.cj:332:5`

### `wirestack.http.HttpClient.default`

```cj
public static func default(): HttpClient
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- function · public
- `src/http/client.cj:266:5`

### `wirestack.http.HttpClient.get`

```cj
public func get(url: String,
        context!: OperationContext = OperationContext.background()): HttpResponse
```

Wirestack 公开 API：返回与当前对象关联的值。

#### 参数

- `context` — 控制本次操作的截止时间、取消和追踪范围。
- `url` — 调用方提供的输入值。

- function · public
- `src/http/client.cj:277:5`

### `wirestack.http.HttpClient.getControlled`

```cj
public func getControlled(
        url: String,
        cancellation: HttpCancellationHandles,
        context!: OperationContext = OperationContext.background()
    ): HttpResponse
```

Wirestack 公开 API：返回与当前对象关联的值。

#### 参数

- `cancellation` — 调用方提供的输入值。
- `context` — 控制本次操作的截止时间、取消和追踪范围。
- `url` — 调用方提供的输入值。

- function · public
- `src/http/client.cj:288:5`

### `wirestack.http.HttpClient.isClosed`

```cj
public func isClosed(): Bool
```

Wirestack 公开 API：报告资源是否已经关闭。

- function · public
- `src/http/client.cj:599:5`

### `wirestack.http.HttpClient.send`

```cj
public func send(request: HttpRequest,
        context!: OperationContext = OperationContext.background(),
        trailer!: HttpTrailer = HttpTrailer()
    ): HttpResponse
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `context` — 控制本次操作的截止时间、取消和追踪范围。
- `request` — 待处理的 HTTP 请求。
- `trailer` — 调用方提供的输入值。

- function · public
- `src/http/client.cj:311:5`

### `wirestack.http.HttpClient.sendControlled`

```cj
public func sendControlled(
        request: HttpRequest,
        cancellation: HttpCancellationHandles,
        context!: OperationContext = OperationContext.background(),
        trailer!: HttpTrailer = HttpTrailer()
    ): HttpResponse
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `cancellation` — 调用方提供的输入值。
- `context` — 控制本次操作的截止时间、取消和追踪范围。
- `request` — 待处理的 HTTP 请求。
- `trailer` — 调用方提供的输入值。

- function · public
- `src/http/client.cj:441:5`

### `wirestack.http.HttpClient.upgrade`

```cj
public func upgrade(request: HttpRequest, protocol!: ?String = None<String>,
        context!: OperationContext = OperationContext.background()): HttpUpgradeResult
```

Transfers a successful HTTP/1\.1 Upgrade or CONNECT connection without replay\.
HTTPS may negotiate HTTP/2, where the exchange uses a stream\-scoped CONNECT\.

#### 参数

- `request` — Bodyless CONNECT or HTTP/1\.1 Upgrade handshake request\.
- `protocol` — Required extended CONNECT protocol offered by Upgrade; absent only for ordinary CONNECT\.
- `context` — Shared operation deadline and cancellation\.

- function · public
- `src/http/client.cj:359:5`

