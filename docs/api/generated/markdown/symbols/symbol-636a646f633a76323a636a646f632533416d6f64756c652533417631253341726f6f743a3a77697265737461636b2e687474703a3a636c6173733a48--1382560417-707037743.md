# `wirestack.http.HttpRedirectPolicy`

[包 `wirestack.http`](../packages/package-32303a636a646f633a6d6f64756c653a76313a726f6f7477697265737461636b2e68747470.md)

```cj
public class HttpRedirectPolicy
```

Immutable redirect policy\. Decisions are made before the prior response is drained\.

## 成员

### `wirestack.http.HttpRedirectPolicy.init`

```cj
public init(maximumRedirects!: Int64 = 10, allowHttpsDowngrade!: Bool = false,
        maximumDrainBytes!: Int64 = 64 * 1024)
```

Wirestack 公开 API：构造并校验该公开值。

#### 参数

- `allowHttpsDowngrade` — 调用方提供的输入值。
- `maximumDrainBytes` — 调用方提供的输入值。
- `maximumRedirects` — 调用方提供的输入值。

- constructor · public
- `src/http/redirect.cj:79:5`

### `wirestack.http.HttpRedirectPolicy.nextRequest`

```cj
public func nextRequest(request: HttpRequest, response: HttpResponse,
        redirectsFollowed!: Int64 = 0, limits!: HttpLimits = HttpLimits()): ?HttpRequest
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `limits` — 调用方提供的输入值。
- `redirectsFollowed` — 调用方提供的输入值。
- `request` — 待处理的 HTTP 请求。
- `response` — 处理后的 HTTP 响应。

- function · public
- `src/http/redirect.cj:96:5`

### `wirestack.http.HttpRedirectPolicy.allowHttpsDowngrade`

```cj
public let allowHttpsDowngrade: Bool
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http/redirect.cj:67:5`

### `wirestack.http.HttpRedirectPolicy.maximumDrainBytes`

```cj
public let maximumDrainBytes: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http/redirect.cj:71:5`

### `wirestack.http.HttpRedirectPolicy.maximumRedirects`

```cj
public let maximumRedirects: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http/redirect.cj:63:5`

