# `wirestack.TrustVerificationEvidence`

[包 `wirestack`](../packages/package-32303a636a646f633a6d6f64756c653a76313a726f6f7477697265737461636b.md)

```cj
public struct TrustVerificationEvidence
```

Stable diagnostic result with no native certificate/provider object\.

## 成员

### `wirestack.TrustVerificationEvidence.init`

```cj
public init(
        status: TrustVerificationStatus,
        trustSource: TrustAnchorSource,
        trustContextIdentity: String,
        peerChainLength: Int64,
        matchedPinIndex!: ?Int64 = None<Int64>,
        verifiedReferenceIdentity!: ?String = None<String>
    )
```

Wirestack 公开 API：构造并校验该公开值。

#### 参数

- `matchedPinIndex` — 调用方提供的输入值。
- `peerChainLength` — 调用方提供的输入值。
- `status` — 待记录的协议状态码。
- `trustContextIdentity` — 调用方提供的输入值。
- `trustSource` — 调用方提供的输入值。
- `verifiedReferenceIdentity` — 调用方提供的输入值。

- constructor · public
- `src/trust.cj:522:5`

### `wirestack.TrustVerificationEvidence.matchedPinIndex`

```cj
public let matchedPinIndex: ?Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/trust.cj:507:5`

### `wirestack.TrustVerificationEvidence.peerChainLength`

```cj
public let peerChainLength: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/trust.cj:503:5`

### `wirestack.TrustVerificationEvidence.status`

```cj
public let status: TrustVerificationStatus
```

Wirestack 公开 API：返回当前对象保存的受约束值。

- variable · public
- `src/trust.cj:491:5`

### `wirestack.TrustVerificationEvidence.trustContextIdentity`

```cj
public let trustContextIdentity: String
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/trust.cj:499:5`

### `wirestack.TrustVerificationEvidence.trustSource`

```cj
public let trustSource: TrustAnchorSource
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/trust.cj:495:5`

### `wirestack.TrustVerificationEvidence.verifiedReferenceIdentity`

```cj
public let verifiedReferenceIdentity: ?String
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/trust.cj:511:5`

