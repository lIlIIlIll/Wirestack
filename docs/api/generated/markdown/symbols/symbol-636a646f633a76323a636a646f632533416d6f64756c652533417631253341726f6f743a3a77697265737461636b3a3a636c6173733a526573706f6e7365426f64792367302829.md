# `wirestack.ResponseBody`

[包 `wirestack`](../packages/package-32303a636a646f633a6d6f64756c653a76313a726f6f7477697265737461636b.md)

```cj
public class ResponseBody <: Resource
```

Single\-owner response body\. EOF completes reuse eligibility; an early close abandons it\.
Exactly one observer callback is made, including when reading or closing fails\.

## 成员

### `wirestack.ResponseBody.init`

```cj
public init(
        stream: HttpBodyStream,
        observer!: ResponseBodyObserver = NoopResponseBodyObserver(),
        trailerProvider!: () -> ?HttpTrailer = { => None<HttpTrailer> },
        completeAtDeclaredLength!: Bool = true
    )
```

Wirestack 公开 API：构造并校验该公开值。

#### 参数

- `completeAtDeclaredLength` — 调用方提供的输入值。
- `observer` — 调用方提供的输入值。
- `stream` — 提供连续数据的输入流。
- `trailerProvider` — 调用方提供的输入值。

- constructor · public
- `src/http_message.cj:148:5`

### `wirestack.ResponseBody.close`

```cj
public func close(): Unit
```

Wirestack 公开 API：关闭资源并唤醒正在等待的操作。

- function · public
- `src/http_message.cj:247:5`

### `wirestack.ResponseBody.drain`

```cj
public func drain(maximumBytes: Int64, bufferSize!: Int64 = 16 * 1024): Bool
```

Consumes and closes at most `maximumBytes` of unread body data\.
Returns true only when EOF was proven and the connection may remain reusable\.

#### 参数

- `bufferSize` — 调用方提供的输入值。
- `maximumBytes` — 允许读取的最大字节数。

- function · public
- `src/http_message.cj:223:5`

### `wirestack.ResponseBody.empty`

```cj
public static func empty(observer!: ResponseBodyObserver = NoopResponseBodyObserver()): ResponseBody
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `observer` — 调用方提供的输入值。

- function · public
- `src/http_message.cj:164:5`

### `wirestack.ResponseBody.isClosed`

```cj
public func isClosed(): Bool
```

Wirestack 公开 API：报告资源是否已经关闭。

- function · public
- `src/http_message.cj:267:5`

### `wirestack.ResponseBody.isFullyConsumed`

```cj
public func isFullyConsumed(): Bool
```

Wirestack 公开 API：报告当前对象是否满足对应条件。

- function · public
- `src/http_message.cj:272:5`

### `wirestack.ResponseBody.read`

```cj
public func read(destination: Array<Byte>): Int64
```

Wirestack 公开 API：读取下一段数据并返回实际进度。

#### 参数

- `destination` — 调用方提供的输入值。

- function · public
- `src/http_message.cj:172:5`

### `wirestack.ResponseBody.completesAtDeclaredLength`

```cj
public prop completesAtDeclaredLength: Bool
```

Whether consuming the declared length proves protocol completion without a later EOF\.

- property · public
- `src/http_message.cj:139:5`

### `wirestack.ResponseBody.contentLength`

```cj
public prop contentLength: ?Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- property · public
- `src/http_message.cj:133:5`

### `wirestack.ResponseBody.trailer`

```cj
public prop trailer: ?HttpTrailer
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- property · public
- `src/http_message.cj:137:5`

