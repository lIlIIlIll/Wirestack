# `wirestack.HttpLimits`

[包 `wirestack`](../packages/package-32303a636a646f633a6d6f64756c653a76313a726f6f7477697265737461636b.md)

```cj
public class HttpLimits
```

Parser and serializer bounds shared by the client and server paths\.

## 成员

### `wirestack.HttpLimits.init`

```cj
public init(
        maxHeaderLineBytes!: Int64 = 8192,
        maxHeaderBytes!: Int64 = 65536,
        maxHeaderCount!: Int64 = 100,
        maxChunkLineBytes!: Int64 = 8192,
        maxTrailerBytes!: Int64 = 16384,
        maxTrailerCount!: Int64 = 32,
        maxBodyBytes!: Int64 = 1073741824
    )
```

Wirestack 公开 API：构造并校验该公开值。

#### 参数

- `maxBodyBytes` — 调用方提供的输入值。
- `maxChunkLineBytes` — 调用方提供的输入值。
- `maxHeaderBytes` — 调用方提供的输入值。
- `maxHeaderCount` — 调用方提供的输入值。
- `maxHeaderLineBytes` — 调用方提供的输入值。
- `maxTrailerBytes` — 调用方提供的输入值。
- `maxTrailerCount` — 调用方提供的输入值。

- constructor · public
- `src/http_model.cj:114:5`

### `wirestack.HttpLimits.maxBodyBytes`

```cj
public let maxBodyBytes: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http_model.cj:102:5`

### `wirestack.HttpLimits.maxChunkLineBytes`

```cj
public let maxChunkLineBytes: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http_model.cj:90:5`

### `wirestack.HttpLimits.maxHeaderBytes`

```cj
public let maxHeaderBytes: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http_model.cj:82:5`

### `wirestack.HttpLimits.maxHeaderCount`

```cj
public let maxHeaderCount: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http_model.cj:86:5`

### `wirestack.HttpLimits.maxHeaderLineBytes`

```cj
public let maxHeaderLineBytes: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http_model.cj:78:5`

### `wirestack.HttpLimits.maxTrailerBytes`

```cj
public let maxTrailerBytes: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http_model.cj:94:5`

### `wirestack.HttpLimits.maxTrailerCount`

```cj
public let maxTrailerCount: Int64
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- variable · public
- `src/http_model.cj:98:5`

