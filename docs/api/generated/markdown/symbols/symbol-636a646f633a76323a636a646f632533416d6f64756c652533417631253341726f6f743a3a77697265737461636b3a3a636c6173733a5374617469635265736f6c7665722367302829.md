# `wirestack.StaticResolver`

[包 `wirestack`](../packages/package-32303a636a646f633a6d6f64756c653a76313a726f6f7477697265737461636b.md)

```cj
public class StaticResolver <: Resolver
```

Deterministic resolver used by connector, TLS and HTTP tests\.

## 成员

### `wirestack.StaticResolver.init`

```cj
public init(result: ResolveResult)
```

Wirestack 公开 API：构造并校验该公开值。

#### 参数

- `result` — 调用方提供的输入值。

- constructor · public
- `src/resolver.cj:338:5`

### `wirestack.StaticResolver.resolve`

```cj
public func resolve(
        host: HostName,
        service: ?String,
        options: ResolveOptions,
        context: OperationContext
    ): ResolveResult
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `context` — 控制本次操作的截止时间、取消和追踪范围。
- `host` — 待解析的主机名或地址字面量。
- `options` — 解析或连接时使用的选项。
- `service` — 服务名称或端口别名。

- function · public
- `src/resolver.cj:349:5`

### `wirestack.StaticResolver.resolve`

```cj
public func resolve(
        host: HostName,
        options!: ResolveOptions = ResolveOptions(),
        context!: OperationContext = OperationContext.background()
    ): ResolveResult
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `context` — 控制本次操作的截止时间、取消和追踪范围。
- `host` — 待解析的主机名或地址字面量。
- `options` — 解析或连接时使用的选项。

- function · public
- `src/resolver.cj:385:5`

