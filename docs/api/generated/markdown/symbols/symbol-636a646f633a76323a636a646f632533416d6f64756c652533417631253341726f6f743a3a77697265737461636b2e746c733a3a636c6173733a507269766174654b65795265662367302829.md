# `wirestack.tls.PrivateKeyRef`

[包 `wirestack.tls`](../packages/package-32303a636a646f633a6d6f64756c653a76313a726f6f7477697265737461636b2e746c73.md)

```cj
public class PrivateKeyRef <: Resource
```

Public owner of a provider\-neutral private\-key reference\.

## 成员

### `wirestack.tls.PrivateKeyRef.close`

```cj
public func close(): Unit
```

Wirestack 公开 API：关闭资源并唤醒正在等待的操作。

- function · public
- `src/tls/identity.cj:87:5`

### `wirestack.tls.PrivateKeyRef.externalSigner`

```cj
public static func externalSigner(
        referenceIdentity: String,
        subjectPublicKeyInfoDer: Array<Byte>,
        signer: ExternalSigner
    ): PrivateKeyRef
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `referenceIdentity` — 连接所期望的身份。
- `signer` — 调用方提供的输入值。
- `subjectPublicKeyInfoDer` — 调用方提供的输入值。

- function · public
- `src/tls/identity.cj:48:5`

### `wirestack.tls.PrivateKeyRef.isClosed`

```cj
public func isClosed(): Bool
```

Wirestack 公开 API：报告资源是否已经关闭。

- function · public
- `src/tls/identity.cj:91:5`

### `wirestack.tls.PrivateKeyRef.pkcs8`

```cj
public static func pkcs8(bytes: Array<Byte>): PrivateKeyRef
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `bytes` — 保存原始内容的字节序列。

- function · public
- `src/tls/identity.cj:30:5`

### `wirestack.tls.PrivateKeyRef.pkcs8File`

```cj
public static func pkcs8File(path: String): PrivateKeyRef
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `path` — 要打开或保存的文件系统路径。

- function · public
- `src/tls/identity.cj:38:5`

### `wirestack.tls.PrivateKeyRef.systemHandle`

```cj
public static func systemHandle(
        platform: String,
        alias: String,
        subjectPublicKeyInfoDer: Array<Byte>,
        signer: ExternalSigner,
        hardwareBacked!: Bool = true
    ): PrivateKeyRef
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

#### 参数

- `alias` — 调用方提供的输入值。
- `hardwareBacked` — 调用方提供的输入值。
- `platform` — 调用方提供的输入值。
- `signer` — 调用方提供的输入值。
- `subjectPublicKeyInfoDer` — 调用方提供的输入值。

- function · public
- `src/tls/identity.cj:68:5`

### `wirestack.tls.PrivateKeyRef.kind`

```cj
public prop kind: PrivateKeyKind
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- property · public
- `src/tls/identity.cj:16:5`

### `wirestack.tls.PrivateKeyRef.referenceIdentity`

```cj
public prop referenceIdentity: String
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- property · public
- `src/tls/identity.cj:20:5`

### `wirestack.tls.PrivateKeyRef.requiresHardwareKey`

```cj
public prop requiresHardwareKey: Bool
```

Wirestack 公开 API：公开成员，用于在网络操作中传递受约束的状态。

- property · public
- `src/tls/identity.cj:24:5`

